{
  "routes": [
    {
      "method": "GET",
      "path": "/somethings",
      "handler": "something.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/somethings/count",
      "handler": "something.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/somethings/:id",
      "handler": "something.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/somethings",
      "handler": "something.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/somethings/:id",
      "handler": "something.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/somethings/:id",
      "handler": "something.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
